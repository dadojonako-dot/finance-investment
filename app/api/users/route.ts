import {NextResponse} from 'next/server';import {prisma} from '../../../src/lib/prisma';const roles=['OWNER','ADMIN','FINANCE','TRADER','ACCOUNTANT','VIEWER'];export async function GET(){try{return NextResponse.json(await prisma.user.findMany({select:{id:true,email:true,name:true,role:true,isActive:true,createdAt:true},orderBy:{createdAt:'asc'}}))}catch{return NextResponse.json({error:'Не удалось получить пользователей'},{status:500})}}export async function POST(req:Request){try{const b=await req.json();if(!b.email||!b.name||!roles.includes(b.role))return NextResponse.json({error:'Укажите имя, email и роль'},{status:400});const row=await prisma.user.create({data:{email:String(b.email).toLowerCase(),name:b.name,role:b.role,passwordHash:'PENDING_ACTIVATION',isActive:b.isActive!==false}});const actor=await prisma.user.findFirst({where:{isActive:true},orderBy:{createdAt:'asc'}});if(actor)await prisma.auditLog.create({data:{userId:actor.id,entityType:'User',entityId:row.id,action:'CREATE_USER',newValue:{email:row.email,name:row.name,role:row.role,isActive:row.isActive}}});return NextResponse.json({id:row.id,email:row.email,name:row.name,role:row.role,isActive:row.isActive},{status:201})}catch{return NextResponse.json({error:'Не удалось создать пользователя'},{status:500})}}
