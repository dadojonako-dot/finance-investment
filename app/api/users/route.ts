import {apiError} from '../../../src/lib/api-error';
import {NextResponse} from 'next/server';import {prisma} from '../../../src/lib/prisma';import {authorize,hashPassword} from '../../../src/auth/server';const roles=['OWNER','ADMIN','FINANCE','TRADER','ACCOUNTANT','VIEWER'];export async function GET(){const auth=await authorize('users.manage');if(auth.response)return auth.response;try{return NextResponse.json(await prisma.user.findMany({select:{id:true,email:true,name:true,role:true,isActive:true,createdAt:true},orderBy:{createdAt:'asc'}}))}catch{return NextResponse.json({error:'Не удалось получить пользователей'},{status:500})}}export async function POST(req:Request){const auth=await authorize('users.manage');if(auth.response)return auth.response;try{const b=await req.json(),password=String(b.password||'');if(typeof b.email!=='string'||!/^\S+@\S+\.\S+$/.test(b.email)||b.email.length>254||typeof b.name!=='string'||!b.name.trim()||b.name.length>200||!roles.includes(b.role))return NextResponse.json({error:'Укажите имя, email, роль и пароль не короче 10 символов'},{status:400});if(b.role==='OWNER'&&auth.user.role!=='OWNER')return NextResponse.json({error:'Только OWNER может создавать другого OWNER'},{status:403});const result=await prisma.$transaction(async tx=>{const row=await tx.user.create({data:{email:String(b.email).toLowerCase(),name:b.name,role:b.role,passwordHash:await hashPassword(password),isActive:b.isActive!==false}});await tx.auditLog.create({data:{userId:auth.user.id,entityType:'User',entityId:row.id,action:'CREATE_USER',newValue:{email:row.email,name:row.name,role:row.role,isActive:row.isActive}}});return {id:row.id,email:row.email,name:row.name,role:row.role,isActive:row.isActive}});return NextResponse.json(result,{status:201})}catch(error){return apiError(error)}}export async function PATCH(req:Request){const auth=await authorize('users.manage');if(auth.response)return auth.response;try{const b=await req.json();if(!b.id)return NextResponse.json({error:'Не указан пользователь'},{status:400});return await prisma.$transaction(async tx=>{await tx.$executeRaw`SELECT pg_advisory_xact_lock(741002)`;const actor=await tx.user.findUnique({where:{id:auth.user.id}});if(!actor?.isActive||!['OWNER','ADMIN'].includes(actor.role))return NextResponse.json({error:'Недостаточно прав'},{status:403});const current=await tx.user.findUnique({where:{id:b.id}});if(!current)return NextResponse.json({error:'Пользователь не найден'},{status:404});if(current.role==='OWNER'&&actor.role!=='OWNER')return NextResponse.json({error:'Только OWNER может изменять OWNER'},{status:403});if(b.role==='OWNER'&&actor.role!=='OWNER')return NextResponse.json({error:'Только OWNER может назначать роль OWNER'},{status:403});if(b.role&&!roles.includes(b.role))return NextResponse.json({error:'Некорректная роль'},{status:400});if(current.id===auth.user.id&&b.isActive===false)return NextResponse.json({error:'Нельзя отключить собственную учетную запись'},{status:400});if(current.role==='OWNER'&&current.isActive&&((b.role&&b.role!=='OWNER')||b.isActive===false)&&await tx.user.count({where:{role:'OWNER',isActive:true}})<=1)return NextResponse.json({error:'Нельзя удалить последнего активного OWNER'},{status:400});const data:any={};if(b.name!==undefined)data.name=String(b.name);if(b.role!==undefined)data.role=b.role;if(b.isActive!==undefined){if(typeof b.isActive!=='boolean')return NextResponse.json({error:'Некорректный статус'},{status:400});data.isActive=b.isActive;}if(b.password!==undefined){data.passwordHash=await hashPassword(String(b.password))}if(data.passwordHash||data.isActive===false)data.sessionVersion={increment:1};const row=await tx.user.update({where:{id:b.id},data});const events=[];
if(current.role!==row.role)events.push({action:'UPDATE_ROLE',oldValue:{role:current.role},newValue:{role:row.role}});
if(current.isActive!==row.isActive)events.push({action:row.isActive?'ENABLE_USER':'DISABLE_USER',oldValue:{isActive:current.isActive},newValue:{isActive:row.isActive}});
if(data.passwordHash)events.push({action:'RESET_PASSWORD',oldValue:{},newValue:{sessionsRevoked:true}});
if(current.name!==row.name)events.push({action:'UPDATE_USER',oldValue:{name:current.name},newValue:{name:row.name}});
for(const event of events)await tx.auditLog.create({data:{userId:auth.user.id,entityType:'User',entityId:row.id,...event}});return NextResponse.json({id:row.id,email:row.email,name:row.name,role:row.role,isActive:row.isActive})})}catch(error){return apiError(error)}}
