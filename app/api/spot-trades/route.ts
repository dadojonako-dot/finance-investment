import {NextResponse} from 'next/server';import {prisma} from '../../../src/lib/prisma';
export async function GET(req:Request){try{const u=new URL(req.url),ledgerId=u.searchParams.get('ledgerId')||undefined;const rows=await prisma.spotTrade.findMany({where:{ledgerId},include:{ledger:true},orderBy:{tradeDate:'desc'},take:500});return NextResponse.json(rows)}catch{return NextResponse.json({error:'Не удалось получить Spot сделки'},{status:500})}}
export async function POST(req:Request){try{const b=await req.json();const quantity=Number(b.quantity),price=Number(b.price),fee=Number(b.fee||0);if(!b.ledgerId||!b.exchangeAccountId||!b.symbol||!['BUY','SELL'].includes(b.side)||quantity<=0||price<=0)return NextResponse.json({error:'Заполните Ledger, биржевой счет, пару, сторону, количество и цену'},{status:400});const account=await prisma.account.findUnique({where:{id:b.exchangeAccountId}});if(!account||account.ledgerId!==b.ledgerId)return NextResponse.json({error:'Биржевой счет не относится к выбранному Ledger'},{status:400});const row=await prisma.spotTrade.create({data:{ledgerId:b.ledgerId,exchangeAccountId:b.exchangeAccountId,symbol:String(b.symbol).toUpperCase(),side:b.side,quantity,price,fee,realizedPnl:b.realizedPnl==null?null:Number(b.realizedPnl),tradeDate:b.tradeDate?new Date(b.tradeDate):new Date(),notes:b.notes||null}});return NextResponse.json(row,{status:201})}catch{return NextResponse.json({error:'Не удалось сохранить Spot сделку'},{status:500})}}
