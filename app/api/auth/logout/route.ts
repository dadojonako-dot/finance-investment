import {NextResponse} from 'next/server';import {AUTH_COOKIE,getCurrentUser} from '../../../../src/auth/server';import {prisma} from '../../../../src/lib/prisma';export async function POST(){const user=await getCurrentUser();if(user)await prisma.auditLog.create({data:{userId:user.id,entityType:'Auth',entityId:user.id,action:'LOGOUT'}});const res=NextResponse.json({ok:true});res.cookies.set({name:AUTH_COOKIE,value:'',path:'/',maxAge:0});return res}
