import {NextResponse} from 'next/server';import {Prisma} from '@prisma/client';import {prisma} from '../../../src/lib/prisma';const PROJECT_REQUIRED=['INCOME','EXPENSE'];const ALLOWED=['INCOME','EXPENSE','INVESTMENT','INVESTMENT_RETURN','COMMISSION','ADJUSTMENT'];const negative=new Set(['EXPENSE','INVESTMENT','COMMISSION']);export async function GET(req:Request){const u=new URL(req.url);const ledgerId=u.searchParams.get('ledgerId')||undefined;const projectId=u.searchParams.get('projectId')||undefined;const rows=await prisma.transaction.findMany({where:{ledgerId,projectId},include:{ledger:true,account:{include:{asset:true}},project:true,createdBy:true},orderBy:{operationDate:'desc'},take:500});return NextResponse.json(rows)}export async function POST(req:Request){try{const b=await req.json();if(!b.ledgerId||!b.accountId||!b.type||!b.assetCode||!b.amount)return NextResponse.json({error:'Заполните Ledger, счет, тип, актив и сумму'},{status:400});if(!ALLOWED.includes(b.type))return NextResponse.json({error:'TRANSFER и FX_EXCHANGE создаются только через специальные формы перевода/обмена'},{status:400});const raw=new Prisma.Decimal(String(b.amount));if(raw.lte(0)&&b.type!=='ADJUSTMENT')return NextResponse.json({error:'Сумма должна быть больше нуля'},{status:400});if(PROJECT_REQUIRED.includes(b.type)&&!b.projectId)return NextResponse.json({error:'Для дохода и расхода проект обязателен'},{status:400});if(b.type==='EXPENSE'&&!b.category)return NextResponse.json({error:'Для расхода категория обязательна'},{status:400});const account=await prisma.account.findUnique({where:{id:b.accountId},include:{asset:true}});if(!account||account.ledgerId!==b.ledgerId)return NextResponse.json({error:'Счет не принадлежит выбранному Ledger'},{status:400});if(account.asset.code.toUpperCase()!==String(b.assetCode).toUpperCase())return NextResponse.json({error:'Актив операции не соответствует активу счета'},{status:400});const user=await prisma.user.findFirst({where:{isActive:true},orderBy:{createdAt:'asc'}});if(!user)return NextResponse.json({error:'Не найден активный пользователь. Сначала настройте администратора.'},{status:409});let amount=b.type==='ADJUSTMENT'?raw:(negative.has(b.type)?raw.neg():raw);const rate=b.usdRate==null||b.usdRate===''?null:new Prisma.Decimal(String(b.usdRate));const usdAmount=rate?amount.mul(rate):null;const row=await prisma.$transaction(async tx=>{const created=await tx.transaction.create({data:{ledgerId:b.ledgerId,accountId:b.accountId,type:b.type,assetCode:String(b.assetCode).toUpperCase(),amount,usdRate:rate,usdAmount,operationDate:b.operationDate?new Date(b.operationDate):new Date(),counterparty:b.counterparty||null,projectId:b.projectId||null,category:b.category||null,description:b.description||null,documentRef:b.documentRef||null,createdById:user.id},include:{project:true,account:true}});await tx.auditLog.create({data:{userId:user.id,entityType:'Transaction',entityId:created.id,action:'CREATE',newValue:{type:created.type,ledgerId:created.ledgerId,accountId:created.accountId,assetCode:created.assetCode,amount:created.amount.toString(),usdAmount:created.usdAmount?.toString()||null,projectId:created.projectId}}});return created});return NextResponse.json(row,{status:201})}catch{return NextResponse.json({error:'Не удалось сохранить операцию'},{status:500})}}
