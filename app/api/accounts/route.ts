import {NextResponse} from 'next/server';import {prisma} from '../../../src/lib/prisma';
export async function GET(req:Request){const ledgerId=new URL(req.url).searchParams.get('ledgerId')||undefined;const rows=await prisma.account.findMany({where:{ledgerId,isActive:true},include:{asset:true,ledger:true},orderBy:{createdAt:'asc'}});return NextResponse.json(rows)}
export async function POST(req:Request){try{const b=await req.json();if(!b.ledgerId||!b.name||!b.assetCode||!b.type)return NextResponse.json({error:'Ledger, название, тип и актив обязательны'},{status:400});const asset=await prisma.asset.findUnique({where:{code:String(b.assetCode).toUpperCase()}});if(!asset)return NextResponse.json({error:'Актив не найден в справочнике'},{status:400});const row=await prisma.account.create({data:{ledgerId:b.ledgerId,name:b.name.trim(),type:b.type,assetId:asset.id,openingBalance:b.openingBalance||0,bankName:b.bankName||null,accountNumber:b.accountNumber||null},include:{asset:true}});return NextResponse.json(row,{status:201})}catch(e){return NextResponse.json({error:'Не удалось создать счет'},{status:500})}}
