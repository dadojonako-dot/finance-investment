import {NextResponse} from 'next/server';import {prisma} from '../../../src/lib/prisma';import {authorize} from '../../../src/auth/server';export async function GET(){const auth=await authorize('transactions.view');if(auth.response)return auth.response;const rows=await prisma.ledger.findMany({where:{status:'ACTIVE'},include:{accounts:{include:{asset:true}}},orderBy:{createdAt:'asc'}});return NextResponse.json(rows)}export async function POST(req:Request){const auth=await authorize('ledgers.manage');if(auth.response)return auth.response;try{const b=await req.json();if(!b.name?.trim())return NextResponse.json({error:'Название Ledger обязательно'},{status:400});const row=await prisma.ledger.create({data:{name:b.name.trim(),description:b.description?.trim()||null,baseCurrency:b.baseCurrency||'USD'}});await prisma.auditLog.create({data:{userId:auth.user.id,entityType:'Ledger',entityId:row.id,action:'CREATE',newValue:{name:row.name,baseCurrency:row.baseCurrency,status:row.status}}});return NextResponse.json(row,{status:201})}catch{return NextResponse.json({error:'Не удалось создать Ledger'},{status:500})}}
