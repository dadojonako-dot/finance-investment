import {NextResponse} from 'next/server';import {Prisma} from '@prisma/client';import {prisma} from '../../../src/lib/prisma';import {authorize} from '../../../src/auth/server';export async function GET(){const auth=await authorize('transactions.view');if(auth.response)return auth.response;return NextResponse.json(await prisma.project.findMany({orderBy:{createdAt:'asc'}}))}export async function POST(req:Request){const auth=await authorize('projects.manage');if(auth.response)return auth.response;try{const b=await req.json();if(!b.name?.trim())return NextResponse.json({error:'Название проекта обязательно'},{status:400});const targetAmount=b.targetAmount==null||b.targetAmount===''?null:new Prisma.Decimal(String(b.targetAmount));const row=await prisma.project.create({data:{name:b.name.trim(),description:b.description||null,status:b.status||'ACTIVE',isGeneral:Boolean(b.isGeneral),startDate:b.startDate?new Date(b.startDate):null,targetAmount}});await prisma.auditLog.create({data:{userId:auth.user.id,entityType:'Project',entityId:row.id,action:'CREATE',newValue:{name:row.name,status:row.status,isGeneral:row.isGeneral,targetAmount:row.targetAmount?.toString()||null}}});return NextResponse.json(row,{status:201})}catch{return NextResponse.json({error:'Не удалось создать проект'},{status:500})}}
