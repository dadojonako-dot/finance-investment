'use client';import {FormEvent,useState} from 'react';export default function Setup(){const[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');const r=await fetch('/api/auth/bootstrap',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password})});const d=await r.json();if(!r.ok){setError(d.error||'Ошибка настройки');return}location.href='/dashboard'}return <main style={root}><section style={card}><div style={brand}>DADO</div><h1>Первичная настройка</h1><p style={muted}>Создайте первого пользователя с ролью OWNER. Этот экран работает только пока в базе нет пользователей.</p><form onSubmit={submit} style={form}><input style={input} placeholder="Имя владельца" value={name} onChange={e=>setName(e.target.value)} required/><input style={input} type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input style={input} type="password" minLength={10} placeholder="Пароль — минимум 10 символов" value={password} onChange={e=>setPassword(e.target.value)} required/><button style={button}>Создать OWNER и войти</button></form>{error&&<p style={err}>{error}</p>}<a href="/login" style={link}>Вернуться ко входу</a></section></main>}const root:React.CSSProperties={minHeight:'100vh',display:'grid',placeItems:'center',background:'#08111d',color:'#edf4ff',fontFamily:'Inter,Arial',padding:24};const card:React.CSSProperties={width:'100%',maxWidth:460,background:'#0f1b2b',border:'1px solid #22344c',borderRadius:18,padding:28};const brand:React.CSSProperties={fontWeight:900,letterSpacing:2,color:'#5aa2ff'};const muted:React.CSSProperties={color:'#8fa2bb',lineHeight:1.5};const form:React.CSSProperties={display:'grid',gap:12,margin:'22px 0'};const input:React.CSSProperties={background:'#0a1421',border:'1px solid #29405e',color:'#fff',borderRadius:10,padding:'13px 14px'};const button:React.CSSProperties={background:'#2f7df6',color:'#fff',border:0,borderRadius:10,padding:'13px 14px',fontWeight:800};const err:React.CSSProperties={color:'#ff7b86'};const link:React.CSSProperties={color:'#7eb4ff',fontSize:13};
